import javax.swing.*;import java.awt.*;public class Ventana extends JInternalFrame{    private JDialog miDialogo;    private JTextField vdenominacion,vcodigo;    public void pantalla() {        JPanel textos= new JPanel(new GridLayout(2,1));        JPanel campos= new JPanel(new GridLayout(2,1));        getContentPane().add(textos,BorderLayout.WEST);        getContentPane().add(campos,BorderLayout.CENTER);        JLabel tcodigo = new JLabel("Codigo: ",JLabel.RIGHT);        textos.add(tcodigo);        vcodigo= new JTextField(12);        JPanel p = new JPanel(new FlowLayout(FlowLayout.LEFT));        p.add(vcodigo);        campos.add(p);        JLabel tdenominacion  = new JLabel ("Denominacion: ",JLabel.RIGHT);        textos.add(tdenominacion);        vdenominacion  = new JTextField(50);        JPanel p1 = new JPanel(new FlowLayout(FlowLayout.LEFT));        p1.add(vdenominacion);        campos.add(p1);    }    public Ventana() {        super("Ventana",                true, //resizable                true, //closable                true, //maximizable                true);//iconifiable        setSize(700,140)   ;  // (x,y)        setLocation(120,120);  //(x,y)        pantalla();    }}